name: Trigger auto deployment for container-app-y43nptv3cmsfc

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'api/**'
    - '.github/workflows/container-app-y43nptv3cmsfc-AutoDeployTrigger-4d35e473-768b-4a14-a3b4-98ec3b455ebd.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPY43NPTV3CMSFC_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPY43NPTV3CMSFC_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPY43NPTV3CMSFC_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/api 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPY43NPTV3CMSFC_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPY43NPTV3CMSFC_REGISTRY_PASSWORD }}
          containerAppName: container-app-y43nptv3cmsfc
          resourceGroup: taskmanagementsystem9467
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


